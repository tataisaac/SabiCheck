<?xml version="1.0" encoding="utf-8"?>
<!-- DEBUG ONLY: allow plain-HTTP to any host so a phone can reach a backend on your
     laptop (e.g. http://192.168.1.20:8080). Release uses the strict main config. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true"/>
</network-security-config>
