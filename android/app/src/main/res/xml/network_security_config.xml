<?xml version="1.0" encoding="utf-8"?>
<!-- Release builds: HTTPS everywhere, except the local dev loopback addresses so an
     emulator can still talk to a backend running on the host machine.
     The debug build overrides this file (src/debug/res/xml) to allow any cleartext
     host, e.g. a laptop on the same Wi-Fi as a test phone. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="false"/>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
